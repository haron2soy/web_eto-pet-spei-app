[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "climate-core"
version = "0.1.0"
description = "Core climate indices engine (ETo, PET, SPEI)"
authors = [{name = "Your Name"}]
dependencies = [
    "numpy",
    "pandas",
    "scipy"
]
